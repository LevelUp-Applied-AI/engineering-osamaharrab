  GNU nano 8.4                                                 docs/sql_read_results.md *                                                         
GROUP BY cohort
HAVING AVG(prereq_score) > 85;

Result:
(Cohorts with av                     
These queries show how SQL can be used to analyze student data,
including ordering results, grouping records, and filtering groups
using the HAVING clause.

